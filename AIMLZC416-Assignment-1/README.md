# AIMLZC416 — Assignment I

Mathematical Foundations for Machine Learning: linear systems (REF/RREF) and power method on covariance data.

## Files

| File | Purpose |
|------|---------|
| `assignment.py` | Full solution with comment lines (Q1 and Q2) |
| `results.txt` | Numerical output from running the script |

## Run

```bash
python3 assignment.py
```

Requires **NumPy** only for Q2.3(d) (`numpy.linalg.eigh`), as allowed in the assignment.

## Submission

Combine your **BITS ID**, this code (screenshots or pasted), and `results.txt` outputs into a single PDF named `BITSID.pdf` per LMS instructions.
